import { Instance } from "cs_script/point_script"

// Runs when the script starts
Instance.Msg("Hello World!") // Print "Hello World!" to the console
Instance.ServerCommand("say Hello World!") // Print "Hello World!" to the chat
